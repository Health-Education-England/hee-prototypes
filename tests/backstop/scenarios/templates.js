/**
 * Generates BackstopJS scenarios array covering HEE prototype templates.
 */

const id = 'templates';
const pathPattern = './app/views/templates/examples/*.html';

module.exports = { id, pathPattern };
